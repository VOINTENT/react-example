import React from "react";
import preloader from "../../../assets/images/loader4.svg";

const Preloader = (props) => {
    return (
        <img src={preloader} alt="Загрузка"/>
    );
};

export default Preloader;